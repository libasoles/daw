{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: the built-in window management defaults, dragging the frameless window from the custom titlebar and double-click-to-maximize, plus the core window/event permissions the frontend needs to intercept a close request and warn about unsaved changes (issue #14).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize"
  ]
}
